var utils = require('rah.utils')

function requireSettings(){
    var settings = {};
    utils.forEachDir(__dirname + '/settings', true, function(file, filePath){
        settings[file.replace('Settings.json', '')] = require(filePath);
    });
    return settings;
}

module.exports = requireSettings();